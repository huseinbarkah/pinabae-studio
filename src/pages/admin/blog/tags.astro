---
export const prerender = false;
import AdminLayout from '../../../layouts/AdminLayout.astro';
---

<AdminLayout title="Tag Blog | Admin Pinabae">
  <div class="mb-8 flex justify-between items-center">
    <div>
      <h1 class="text-2xl font-bold text-dark">Tag Blog</h1>
      <p class="text-text">Kelola tag untuk mengelompokkan artikel blog Anda secara spesifik.</p>
    </div>
    <a href="/admin/blog" class="px-4 py-2 bg-gray-100 text-dark rounded-lg font-medium hover:bg-gray-200 transition-colors flex items-center gap-2">
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m15 18-6-6 6-6"/></svg>
      Kembali ke Blog
    </a>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-8" x-data="tagsData()">
    <!-- Form Tambah Tag -->
    <div class="lg:col-span-1">
      <div class="bg-white p-6 rounded-xl border border-border shadow-sm">
        <h2 class="text-lg font-bold text-dark mb-4">Tambah Tag Baru</h2>
        <form @submit.prevent="addTag" class="space-y-4">
          <div>
            <label class="block text-sm font-medium text-dark mb-1">Nama Tag</label>
            <input type="text" x-model="newTag.name" @input="generateSlug" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary/50 focus:border-primary outline-none" required placeholder="Contoh: Tutorial">
          </div>
          <div>
            <label class="block text-sm font-medium text-dark mb-1">Slug</label>
            <input type="text" x-model="newTag.slug" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary/50 focus:border-primary outline-none bg-gray-50" required placeholder="tutorial">
            <p class="text-xs text-gray-500 mt-1">URL ramah mesin pencari (otomatis terisi).</p>
          </div>
          <button type="submit" class="w-full py-2 bg-primary text-white rounded-lg font-bold hover:bg-orange-600 transition-colors flex justify-center items-center gap-2" :disabled="submitting">
            <span x-show="!submitting">Simpan Tag</span>
            <span x-show="submitting" style="display: none;">
              <svg class="animate-spin h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>
            </span>
          </button>
        </form>
      </div>
    </div>

    <!-- Tabel Tag -->
    <div class="lg:col-span-2">
      <div class="bg-white rounded-xl border border-border shadow-sm overflow-hidden">
        <div class="p-4 border-b border-border bg-gray-50">
          <h2 class="font-semibold text-dark">Daftar Tag</h2>
        </div>

        <div x-show="loading" class="p-12 text-center text-gray-400">
          <svg class="animate-spin h-8 w-8 mx-auto mb-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"><circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle><path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path></svg>
          <p>Memuat tag...</p>
        </div>

        <div x-show="!loading && tags.length === 0" class="p-12 text-center text-gray-500" style="display: none;">
          <p>Belum ada tag. Silakan buat yang baru.</p>
        </div>

        <table x-show="!loading && tags.length > 0" class="w-full text-left" style="display: none;">
          <thead class="bg-gray-50 border-b border-gray-200">
            <tr>
              <th class="px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Nama</th>
              <th class="px-6 py-3 text-xs font-semibold text-gray-500 uppercase tracking-wider">Slug</th>
              <th class="px-6 py-3 text-right text-xs font-semibold text-gray-500 uppercase tracking-wider">Aksi</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-200">
            <template x-for="tag in tags" :key="tag.id">
              <tr class="hover:bg-gray-50 transition-colors">
                <td class="px-6 py-4">
                  <div class="font-medium text-dark" x-text="tag.name"></div>
                </td>
                <td class="px-6 py-4">
                  <div class="text-sm text-gray-500 font-mono" x-text="tag.slug"></div>
                </td>
                <td class="px-6 py-4 text-right">
                  <button @click="deleteTag(tag.id)" class="text-red-500 hover:text-red-700 text-sm font-medium">Hapus</button>
                </td>
              </tr>
            </template>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</AdminLayout>

<script>
  import { supabase } from '../../../lib/supabase';

  document.addEventListener('alpine:init', () => {
    window.Alpine.data('tagsData', () => ({
      tags: [],
      loading: true,
      submitting: false,
      newTag: {
        name: '',
        slug: ''
      },

      async init() {
        await this.loadTags();
      },

      async loadTags() {
        this.loading = true;
        try {
          const { data, error } = await supabase
            .from('blog_tags')
            .select('*')
            .order('name', { ascending: true });
            
          if (error) throw error;
          this.tags = data || [];
        } catch (error) {
          console.error('Error fetching tags:', error);
        } finally {
          this.loading = false;
        }
      },

      generateSlug() {
        this.newTag.slug = this.newTag.name
          .toLowerCase()
          .replace(/[^\w\s-]/g, '')
          .replace(/[\s_-]+/g, '-')
          .replace(/^-+|-+$/g, '');
      },

      async addTag() {
        if (!this.newTag.name || !this.newTag.slug) return;
        this.submitting = true;

        try {
          const { data, error } = await supabase
            .from('blog_tags')
            .insert([{ name: this.newTag.name, slug: this.newTag.slug }])
            .select();

          if (error) throw error;
          
          if (data && data.length > 0) {
            this.tags.push(data[0]);
            this.tags.sort((a, b) => a.name.localeCompare(b.name));
            this.newTag.name = '';
            this.newTag.slug = '';
          }
        } catch (error) {
          console.error('Error adding tag:', error);
          alert('Gagal menambahkan tag. Pesan error: ' + (error.message || JSON.stringify(error)));
        } finally {
          this.submitting = false;
        }
      },

      async deleteTag(id) {
        if (!confirm('Apakah Anda yakin ingin menghapus tag ini?')) return;
        
        try {
          const { error } = await supabase
            .from('blog_tags')
            .delete()
            .eq('id', id);
            
          if (error) throw error;
          this.tags = this.tags.filter(t => t.id !== id);
        } catch (error) {
          console.error('Error deleting tag:', error);
          alert('Gagal menghapus tag.');
        }
      }
    }));
  });
</script>
